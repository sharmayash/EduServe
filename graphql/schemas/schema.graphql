type Query {
	users: [User!]!
	hello: String
}

type Mutation {
	createUser(name: String!, email: String!, password: String!, age: Int!): User!
}

type User {
	_id: ID,
	name: String,
	email: String,
	password: String,
	age: Int,
  createdAt: String,
  updatedAt: String,
}
